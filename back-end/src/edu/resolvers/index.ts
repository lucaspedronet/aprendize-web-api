import { FindClassResolver } from './queries/find-class.resolver';
import { ListClassTypeResolver } from './queries/list-class-type.resolver';

export const Resolvers = [FindClassResolver, ListClassTypeResolver];
